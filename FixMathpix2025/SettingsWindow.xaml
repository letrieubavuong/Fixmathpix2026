<Window x:Class="FixMathpix2025.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FixMathpix2025"
        mc:Ignorable="d"
        Title="Cài đặt" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#FF2D2D30" Foreground="White">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF3F3F46"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border
                  x:Name="Border" 
                  Grid.ColumnSpan="2"
                  CornerRadius="3"
                  Background="#FF3F3F46"
                  BorderBrush="#FF555555"
                  BorderThickness="1" />
                <Border 
                  Grid.Column="0"
                  CornerRadius="3,0,0,3" 
                  Margin="1" 
                  Background="Transparent" />
                <Path 
                  x:Name="Arrow"
                  Grid.Column="1"     
                  Fill="White"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M 0 0 L 4 4 L 8 0 Z"
                />
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter TargetName="Border" Property="Background" Value="#FF555555" />
                </Trigger>
                <Trigger Property="IsChecked" Value="true">
                    <Setter TargetName="Border" Property="Background" Value="#FF555555" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="DarkComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton 
                                Name="ToggleButton" 
                                Template="{StaticResource ComboBoxToggleButton}" 
                                Grid.Column="2" 
                                Focusable="false"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press">
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                Margin="3,3,23,3"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left" />
                            <Popup 
                                Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True" 
                                Focusable="False"
                                PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#FF3F3F46" BorderThickness="1" BorderBrush="#FF555555"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#FF555555"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3F3F46"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <local:HexToBrushConverter x:Key="HexToBrushConverter"/>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="Transparent" CornerRadius="3,3,0,0" Margin="0,0,2,0">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF3F3F46" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FF555555" />
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#FF252526" />
                                <Setter Property="Foreground" Value="#FF888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <TabItem Header="Soạn thảo">
                <StackPanel Margin="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Label Grid.Row="0" Grid.Column="0" Content="Font chữ:"/>
                        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="FontFamilyComboBox" Margin="5" Style="{StaticResource DarkComboBoxStyle}"/>

                        <Label Grid.Row="0" Grid.Column="2" Content="Cỡ chữ:" Margin="10,0,0,0"/>
                        <ComboBox Grid.Row="0" Grid.Column="3" x:Name="FontSizeComboBox" Margin="5" IsEditable="True" Style="{StaticResource DarkComboBoxStyle}"/>

                        <CheckBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" x:Name="AutoSaveCheckBox" Content="Tự động sao lưu phiên làm việc" Margin="5,10,0,0" VerticalAlignment="Center"/>

                        <Label Grid.Row="2" Grid.Column="0" Content="Thời gian tự động lưu (giây):" Margin="5,10,0,0"/>
                        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="AutoSaveIntervalComboBox" Margin="5,10,5,0" IsEditable="True" Style="{StaticResource DarkComboBoxStyle}"/>
                    </Grid>
                </StackPanel>
            </TabItem>
            <TabItem Header="Màu sắc">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ColorItemsControl" Margin="5">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Content="{Binding Name}" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" Text="{Binding ColorHex, UpdateSourceTrigger=PropertyChanged}" Width="100" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    <Border Grid.Column="2" Width="20" Height="20" Margin="10,0" BorderBrush="Gray" BorderThickness="1" Background="{Binding ColorHex, Converter={StaticResource HexToBrushConverter}}"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Phím tắt">
                <ListView x:Name="ShortcutListView" Background="#FF2D2D30" Foreground="White" BorderThickness="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDuplicate}" Value="True">
                                    <Setter Property="Background" Value="#55FF4444"/> <!-- Màu đỏ nhạt để cảnh báo -->
                                    <Setter Property="ToolTip" Value="{Binding DuplicateMessage}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridView.ColumnHeaderContainerStyle>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Background" Value="#FF3F3F46"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                    <Setter Property="BorderBrush" Value="#FF555555"/>
                                </Style>
                            </GridView.ColumnHeaderContainerStyle>
                            <GridViewColumn Header="Chức năng" Width="250" DisplayMemberBinding="{Binding FunctionName}"/>
                            <GridViewColumn Header="Phím tắt" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding KeyGesture, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 PreviewKeyDown="ShortcutTextBox_PreviewKeyDown"
                                                 ToolTip="Nhấp vào và nhấn tổ hợp phím mới"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
            <TabItem Header="Hướng dẫn">
                <ScrollViewer Margin="10" VerticalScrollBarVisibility="Auto">
                    <TextBlock TextWrapping="Wrap" LineHeight="20">
                        <Bold>Chào mừng bạn đến với FixMathpix 2025!</Bold>
                        <LineBreak/><LineBreak/>
                        Dưới đây là hướng dẫn sử dụng một số chức năng chính của ứng dụng:
                        <LineBreak/><LineBreak/>
                        <Bold>1. Các phím tắt hữu ích:</Bold>
                        <LineBreak/>
                        - <Bold>Ctrl + O:</Bold> Mở một tệp TeX mới.
                        <LineBreak/>
                        - <Bold>Ctrl + S:</Bold> Lưu tệp TeX hiện tại.
                        <LineBreak/>
                        - <Bold>Ctrl + F / Ctrl + H:</Bold> Mở bảng tìm kiếm và thay thế.
                        <LineBreak/>
                        - <Bold>Ctrl + /:</Bold> Chuyển đổi một dòng hoặc khối văn bản thành ghi chú (comment) và ngược lại.
                        <LineBreak/>
                        - <Bold>Ctrl + Shift + U:</Bold> Viết HOA toàn bộ đoạn văn bản được chọn.
                        <LineBreak/>
                        - <Bold>Ctrl + Alt + L:</Bold> Dọn dẹp văn bản (sửa lỗi dấu câu, xóa khoảng trắng thừa...).
                        <LineBreak/><LineBreak/>
                        <Bold>2. Menu chuột phải:</Bold>
                        <LineBreak/>
                        Nhấp chuột phải vào trình soạn thảo để truy cập nhanh các chức năng như Cắt, Sao chép, Dán, Viết hoa, Dọn dẹp văn bản, và nhiều hơn nữa.
                        <LineBreak/><LineBreak/>
                        <Bold>3. Tùy chỉnh:</Bold>
                        <LineBreak/>
                        Bạn có thể thay đổi Font chữ, màu sắc và các phím tắt trong các tab tương ứng của cửa sổ Cài đặt này để phù hợp với sở thích cá nhân.
                    </TextBlock>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Lưu" Click="SaveButton_Click"/>
            <Button Content="Áp dụng" Click="ApplyButton_Click"/>
            <Button Content="Hủy" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
